name: flutter_bus
description: "flutter_package for An FlutterBus for Flutter apps"
version: 0.2.0+1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"
  flutter: 3.7.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mindful_analysis: ^1.0.0+2
  mocktail: ^0.3.0
